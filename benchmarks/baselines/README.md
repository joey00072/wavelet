# Reviewed baselines

Store only benchmark JSON files produced by `wavelet benchmark run` and reviewed
for repeatability in this directory. Baseline comparison requires identical
config content, Torch version, and device names.
